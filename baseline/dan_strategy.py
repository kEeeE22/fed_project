# from baseline.avg_strategy import FedAvg

# #mode: unnorm / inverse(a cuong)
# class FedDan(FedAvg):
#   def __init__(self,mode, *args, **kwargs):
#     super().__init__(*args, **kwargs)
#     self.mode = mode
#   def __repr__(self) -> str:
#     return 'FedDan-' + str(self.mode)